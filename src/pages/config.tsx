import React from 'react';

import ConfigScreen from '../components/screens/Configurations';

interface configProps {}

const config = ({}: configProps) => {
  return <ConfigScreen />;
};
export default config;
